# Exercise 1 — Pauli strings and the TFIM

1. Expand the two-qubit Hamiltonian \(H=JZ_0Z_1+h(X_0+X_1)\) as a \(4\times4\) matrix.
2. Verify that every term is Hermitian.
3. Compute \([Z_0Z_1,X_0]\) and \([Z_0Z_1,X_1]\).
4. Use the package to build the \(n=3\) open-chain Hamiltonian.
5. Modify `tfim_components` locally to implement a longitudinal field \(g\sum_i Z_i\).
6. State the Pauli-term count for open and periodic boundary conditions.
